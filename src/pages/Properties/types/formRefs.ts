// types/formRefs.ts
export interface BasicDetailsHandles {
  handleSubmit: () => void;
  isValid: boolean;
}

export interface MediaFORMHandles {
  handleSubmit: () => void;
  isValid: boolean;
}

export interface DiscountHandles {
  handleSubmit: () => void;
  isValid: boolean;
}

export interface PaymentStructureHandles {
  handleSubmit: () => void;
  isValid: boolean;
}

export interface FeaturesInputHandles {
  handleSubmit: () => void;
  isValid: boolean;
}

export interface PropertySpecificationsHandles {
  handleSubmit: () => void;
  isValid: boolean;
}